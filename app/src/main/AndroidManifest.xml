<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pdf.toolkit">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="PDF ToolKit"
        android:theme="@style/Theme.PDFToolKit">

        <!-- AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <!-- Main Activity -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Tool Activities -->
        <activity android:name=".MergePdfActivity" />
        <activity android:name=".SplitPdfActivity" />
        <activity android:name=".CompressPdfActivity" />
        <activity android:name=".PdfToImageActivity" />
        <activity android:name=".ImageToPdfActivity" />
        <activity android:name=".LockPdfActivity" />
        <activity android:name=".UnlockPdfActivity" />
        <activity android:name=".WatermarkPdfActivity" />
        <activity android:name=".RotatePdfActivity" />
        <activity android:name=".ExtractTextActivity" />

    </application>
</manifest>

<?xml version="1.0" encoding="utf-8"?>
<!-- The 'package' attribute has been removed from this tag to comply with modern Android standards. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="PDF Toolkit"
        android:supportsRtl="true"
        android:theme="@style/Theme.PDFToolkit"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            android:name=".RecentFilesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- THIS IS THE FIX. THE CATEGORY IS NOW CORRECTLY SPELLED 'LAUNCHER'. -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

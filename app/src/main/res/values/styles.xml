<resources>

    <!-- Base app theme -->
    <style name="Theme.MyApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your main theme colors -->
        <item name="colorPrimary">#007BFF</item>
        <item name="colorPrimaryVariant">#0056b3</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="android:windowBackground">#E1F5FE</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.MyApp.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
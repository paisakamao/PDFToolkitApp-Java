<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFFFFF">

    <!-- This is the list that will be filled with your recent files -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_recent_files"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- A message to show if the list is empty -->
    <TextView
        android:id="@+id/text_empty_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="No recent files found."
        android:textSize="18sp"
        android:visibility="gone" />

</LinearLayout>

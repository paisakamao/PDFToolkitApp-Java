pluginManagement {
    repositories {
        google()         // 🔹 Required for Android plugin
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()         // 🔹 Required for Android dependencies
        mavenCentral()
    }
}

rootProject.name = "PDFToolkitApp-Java"
include ':app'

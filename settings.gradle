
// This file's only job is to define the project name and include the app module.
// All repository configuration has been removed to resolve the build conflict.

rootProject.name = "PDF Toolkit"
include ':app'
